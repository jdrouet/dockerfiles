stages:
  - build

build-images:
  stage: build
  image: jdrouet/docker-with-buildx:latest
  services:
    - docker:dind
  parallel:
    matrix:
      - PROJECT_FOLDER:
        - docker-with-buildx
        - docker-with-compose
        - node-mjml
        - rust-clippy
        - rust-rustfmt
  script:
    - echo $DOCKER_USERNAME
    - echo $DOCKER_PASSWORD | docker login --username $DOCKER_USERNAME --password-stdin
    - cd $PROJECT_FOLDER
    - EXTRA_ARGS="--push" ./build.sh

